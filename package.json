{
  "name": "roundroom",
  "version": "1.0.0",
  "description": "RoundRoom — NFL Fantasy Draft Assistant. Live Sleeper sync, VORP-based recommendations.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "crawl": "node scripts/crawl.js",
    "aggregate": "node scripts/aggregate.js",
    "vorp": "node scripts/vorp.js",
    "fetch-fantasypros": "node scripts/fetch-fantasypros.js",
    "fetch-underdog": "node scripts/fetch-underdog.js",
    "merge-sources": "node scripts/merge-sources.js",
    "update-data": "node scripts/crawl.js --resume && node scripts/aggregate.js && node scripts/vorp.js && node scripts/fetch-fantasypros.js && node scripts/fetch-underdog.js && node scripts/merge-sources.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.120.0",
    "express": "^4.19.2"
  }
}
