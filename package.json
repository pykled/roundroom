{
  "name": "roundroom",
  "version": "1.0.0",
  "description": "RoundRoom — NFL Fantasy Draft Assistant. Live Sleeper sync, VORP-based recommendations.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "crawl": "node scripts/crawl.js",
    "aggregate": "node scripts/aggregate.js",
    "vorp": "node scripts/vorp.js",
    "update-data": "node scripts/crawl.js && node scripts/aggregate.js && node scripts/vorp.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
